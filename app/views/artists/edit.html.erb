  <% if current_user.id == @artist.user_id %>
<div class="contents row">
    <%= form_tag("/artists/#{@artist.id}", method: :patch) do %>
      <h2>アーティストの編集</h2>
      <ul style="list-style:none;">
      <li>
      <textarea cols="30" rows="1" name="artist_name" placeholder="アーティスト名(必須)" ><%= @artist.artist_name %></textarea>
      </li>
      <li>
      <textarea cols="30" rows="1" name="artist_name_japanese" placeholder="アーティスト名 カナ(必須)" ><%= @artist.artist_name_japanese %></textarea>
      </li>
      <li>
      <textarea cols="30" rows="1" name="artist_url" placeholder="アーティストURL(必須)" ><%= @artist.artist_url %></textarea>
      </li>
      <li>
      <textarea cols="30" rows="1" name="artist_image_url" placeholder="アーティスト画像URL(必須)"><%= @artist.artist_image_url %></textarea>
      </li>
      <li>
      <input type="submit" value="SENT">
      </li>
      </ul>
    <% end %>
    <div class = "name2">※アーティスト画像URLについて:<br/>
      1.アーティストのホームページ上で右クリック。<br/>
      2.「画像アドレスをコピー」をクリック。<br/>
      3.フォームの"アーティスト画像URL"にペースト(貼り付け)してください。
      </div>
</div>
  <% else %>
  <% end %>